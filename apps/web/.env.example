# Dynamia Tasks — development environment
# Copy this file to .env and adjust the paths

# Path to the project you want to use as workspace during development.
# Defaults to the monorepo root if not set.
NUXT_PROJECT_PATH=/home/mario/IdeaProjects/dynamia-tasks

# Optional: IDE callback URL (set automatically by the IntelliJ plugin at runtime)
# NUXT_IDE_CALLBACK_URL=http://localhost:63342

